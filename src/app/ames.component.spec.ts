/*
import {
  beforeEachProviders,
  describe,
  expect,
  it,
  inject
} from '@angular/core/testing';
import { AmesAppComponent } from '../app/ames.component';

beforeEachProviders(() => [AmesAppComponent]);

describe('App: Ames', () => {
  it('should create the app',
      inject([AmesAppComponent], (app: AmesAppComponent) => {
    expect(app).toBeTruthy();
  }));

  it('should have as title \'ames works!\'',
      inject([AmesAppComponent], (app: AmesAppComponent) => {
    expect(app.title).toEqual('ames works!');
  }));
});
*/